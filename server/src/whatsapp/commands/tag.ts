import { FastifyInstance } from 'fastify/types/instance'

function tag(fastify: FastifyInstance, keywords: string[]): void {
  //   console.log(_keywords);
  // TODO: fetch stickers
}

export default tag
