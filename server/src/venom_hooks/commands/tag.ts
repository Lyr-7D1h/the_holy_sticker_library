import { FastifyInstance } from "fastify/types/instance";

function tag(_fastify: FastifyInstance, _keywords: string[]): void {
  //   console.log(_keywords);
  // TODO: fetch stickers
}

export default tag;
