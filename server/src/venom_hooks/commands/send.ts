import { FastifyInstance } from "fastify";

// const send = (fastify: FastifyInstance, keywords: string[]) => {
//   console.log(keywords);
//   // TODO: fetch stickers
// };

// export default send;
